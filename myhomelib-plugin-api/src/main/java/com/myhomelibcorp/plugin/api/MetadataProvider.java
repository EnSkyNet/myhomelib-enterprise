package com.myhomelibcorp.plugin.api;

/** Stable plugin facade for the existing metadata provider SPI. */
public interface MetadataProvider extends com.myhomelibcorp.application.metadata.MetadataProvider {
}
