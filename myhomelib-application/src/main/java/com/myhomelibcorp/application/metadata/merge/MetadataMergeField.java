package com.myhomelibcorp.application.metadata.merge;

/** Metadata fields that may be explicitly selected in an online metadata merge preview. */
public enum MetadataMergeField {
    TITLE,
    AUTHORS,
    ISBN,
    YEAR,
    PUBLISHER,
    LANGUAGE,
    ANNOTATION
}
