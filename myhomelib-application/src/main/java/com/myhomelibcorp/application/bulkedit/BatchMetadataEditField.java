package com.myhomelibcorp.application.bulkedit;

/** Bibliographic fields supported by the local batch editor. TAGS maps to the catalog keywords field. */
public enum BatchMetadataEditField {
    TITLE,
    SERIES,
    PUBLISHER,
    TAGS,
    ANNOTATION,
    LANGUAGE,
    GENRES,
    YEAR
}
