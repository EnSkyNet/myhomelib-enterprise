package com.myhomelibcorp.application.catalog;

/** User-facing update classes produced by the Stage 6 catalog revision model. */
public enum CatalogUpdateType {
    NEW_BY_FOLLOWED_AUTHOR,
    UPDATED_DOWNLOADED_BOOK
}
