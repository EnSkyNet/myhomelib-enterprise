# Iteration 16 — Windows acceptance harness (WIP)

Scope: MHL-011 + MHL-012.

The codebase now contains an evidence-producing Windows acceptance harness for DPI, installer lifecycle and portable Unicode-path smoke. The iteration is intentionally **WIP** because this Linux environment cannot produce or verify real interactive Windows DPI screenshots or a genuine non-admin Windows installer run. See `CONTINUATION-ITERATION-16-WINDOWS-ACCEPTANCE.md`.
