# OPDS

OPDS is a separate module, bound to `127.0.0.1` by default, with paged authors/series/genres/search feeds, book entries, downloads and health. The UI warns before network exposure and can enable Basic Auth.
