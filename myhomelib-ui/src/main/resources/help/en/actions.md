# Commands, hotkeys and book actions

Action Registry owns command IDs, shortcuts, visibility and context predicates. Shortcut conflicts are validated before saving. Book actions are named ordered ProcessBuilder profiles; preview displays argv without executing it.
