Portable mode

Place an empty myhomelib2.ini beside the launcher/JAR. MyHomeLib then stores metadata, collection databases, indexes, cache and application settings relative to the program directory instead of the user profile. This is intended for removable drives and self-contained installations.
