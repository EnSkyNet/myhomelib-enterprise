<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml/1"
            fx:id="mainPane"
            fx:controller="com.myhomelibcorp.ui.controller.MainController"
            style="-fx-background-color: #f0f0f0;">

    <top>
        <VBox>
            <MenuBar>
                <Menu text="Колекція">
                    <MenuItem text="Створити колекцію..." onAction="#handleNewCollection"/>
                    <MenuItem text="Перейменувати колекцію..." onAction="#handleRenameCollection"/>
                    <MenuItem text="Видалити колекцію" onAction="#handleDeleteCollection"/>
                    <MenuItem text="Вибрати колекцію" onAction="#handleSelectCollection"/>
                    <MenuItem text="Майстер створення колекції..." onAction="#handleCollectionWizard"/>
                    <SeparatorMenuItem/>
                    <MenuItem text="Вийти" onAction="#handleExit"/>
                </Menu>
                <Menu text="Група">
                    <MenuItem text="Додати групу..." onAction="#handleAddGroup"/>
                    <MenuItem text="Редагувати групу..." onAction="#handleEditGroup"/>
                    <MenuItem text="Видалити групу" onAction="#handleDeleteGroup"/>
                </Menu>
                <Menu text="Редагування">
                    <MenuItem text="Редагувати метадані..." onAction="#handleEditMetadata"/>
                    <MenuItem text="Видалити книгу" onAction="#handleDeleteBook"/>
                </Menu>
                <Menu text="Вигляд">
                    <MenuItem text="Оновити" onAction="#handleRefresh"/>
                    <SeparatorMenuItem/>
                    <MenuItem text="Показати колонки..." onAction="#handleShowColumns"/>
                </Menu>
                <Menu text="Інструменти">
                    <MenuItem text="Імпорт FB2..." onAction="#handleImportFb2"/>
                    <MenuItem text="Імпорт INPX..." onAction="#handleImportInpx"/>
                    <MenuItem text="Імпорт каталогу..." onAction="#handleImportDirectory"/>
                    <MenuItem text="Експорт..." onAction="#handleExport"/>
                    <SeparatorMenuItem/>
                    <MenuItem text="Перевірити цілісність..." onAction="#handleCheckIntegrity"/>
                    <MenuItem text="Оптимізувати БД (VACUUM)" onAction="#handleVacuum"/>
                    <MenuItem text="Перебудувати індекс..." onAction="#handleRebuildIndex"/>
                    <SeparatorMenuItem/>
                    <MenuItem text="Резервне копіювання..." onAction="#handleBackup"/>
                    <MenuItem text="Відновити з копії..." onAction="#handleRestore"/>
                    <SeparatorMenuItem/>
                    <MenuItem text="Статистика" onAction="#handleStatistics"/>
                    <SeparatorMenuItem/>
                    <MenuItem text="Синхронізувати папку..." onAction="#handleSyncFolder"/>
                    <SeparatorMenuItem/>
                </Menu>
                <Menu text="Допомога">
                    <MenuItem text="Про програму" onAction="#handleAbout"/>
                </Menu>
            </MenuBar>

            <ToolBar>
                <Button fx:id="backButton" text="←" onAction="#handleBack" disable="true"/>
                <Button fx:id="forwardButton" text="→" onAction="#handleForward" disable="true"/>
                <Separator/>
                <Button text="🏠" onAction="#handleHome"/>
                <Separator/>
                <TextField fx:id="searchField" promptText="🔍 Пошук..." prefWidth="300"/>
                <Button text="🔍" onAction="#handleSearch"/>
                <Separator/>
                <Button text="+Імпорт" onAction="#handleImportDirectory"/>
                <Button text="+Книга" onAction="#handleAddBook"/>
                <Separator/>
                <Button text="⭐ Оцінити" onAction="#handleBatchRate" style="-fx-background-color: #FFC107; -fx-text-fill: black;"/>
                <Button text="📖 Прочитано" onAction="#handleBatchMarkRead" style="-fx-background-color: #4CAF50; -fx-text-fill: white;"/>
                <Button text="📂 Додати до групи" onAction="#handleBatchAddToGroup" style="-fx-background-color: #2196F3; -fx-text-fill: white;"/>
                <Button text="✖ Зняти вибір" onAction="#handleClearSelection" style="-fx-background-color: #9E9E9E; -fx-text-fill: white;"/>
                <Button text="🌳" onAction="#handleToggleView" style="-fx-background-color: #9C27B0; -fx-text-fill: white;"/>
                <Separator/>
                <Button text="📤 Експорт" onAction="#handleExport" style="-fx-background-color: #FF9800; -fx-text-fill: white;"/>
                <Button text="📦 INPX" onAction="#handleExportInpx" style="-fx-background-color: #673AB7; -fx-text-fill: white;"/>
                <Separator/>
                <Button text="💾 Backup" onAction="#handleBackup" style="-fx-background-color: #FF9800; -fx-text-fill: white;"/>
                <Separator/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button text="⚙" onAction="#handleSettings"/>
            </ToolBar>
        </VBox>
    </top>

    <left>
        <fx:include source="navigation-panel.fxml"/>
    </left>

    <center>
        <!-- Центральний контейнер, який містить алфавіт та робочий простір -->
        <VBox VBox.vgrow="ALWAYS" style="-fx-background-color: #ffffff;">
            <!-- Алфавітна панель зверху -->
            <fx:include source="alphabet-toolbar-view.fxml"/>

            <!-- Робочий простір (змінюється через WorkspaceManager) -->
            <StackPane fx:id="workspaceStackPane" VBox.vgrow="ALWAYS"/>
        </VBox>
    </center>

    <right>
        <VBox style="-fx-padding: 5; -fx-pref-width: 320; -fx-max-width: 350; -fx-background-color: #fafafa;"
              VBox.vgrow="ALWAYS">
            <fx:include source="details.fxml"/>
        </VBox>
    </right>

    <bottom>
        <fx:include source="statusbar.fxml"/>
    </bottom>
</BorderPane>