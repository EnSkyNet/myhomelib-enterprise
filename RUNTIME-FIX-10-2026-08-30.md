# Runtime fix 10 — 2026-08-30

- Removed catalog-wide transient-root UPDATE from collection startup (728k+ rows in observed runtime).
- Removed syncSeriesFromBooks() from startup and rollback critical paths; series sync remains import-driven.
- Added lazy per-book normalization of legacy cache/catalog-updates roots before online open/download.
- Fixed SqliteCollectionRepositoryCredentialsV7Test schema to include connection_script.
- Added startup-nonblocking regression guard.
