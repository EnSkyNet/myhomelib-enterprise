package com.myhomelibcorp.domain.model.sync;

/** User-owned state that participates in cross-device synchronization. */
public enum SyncEntityType {
    READING_PROGRESS,
    BOOKMARK,
    ANNOTATION,
    RATING,
    GROUP,
    FAVORITE,
    SETTING
}
