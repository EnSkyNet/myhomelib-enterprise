# Runtime fix 11 — online book download

- Restores upstream MyHomeLib online FB2 per-book archive location instead of online.zip/extra.zip.
- Recovers effective collection URL from a legacy bare HTTP(S) ConnectionScript preamble when persisted URL is absent.
- Makes missing URL/script failures visible instead of returning a silent failed future.
- Adds SQLite busy_timeout=5000 to pooled collection/metadata connections.
- Existing broken remote DTO storage is repaired lazily for the selected book and persisted only after successful download.
